input {
    file {
        path => "/logs/*"
        start_position => "beginning"
        codec => "json"
    }
}

output {
  elasticsearch {
        index => "livetooling"
        hosts => ["elasticsearch:9200"]
    }
}
